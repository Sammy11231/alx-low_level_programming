#include "main.h"

/**
 * _abs - program that compute the absolute value of an inter
 * @n: int to be compute
 * Return: Always 0.
 */

int _abs(int n)
{

	if (n >= 0)
	{
		return (n);
	}
	else
	return (-n);
}
