#include "main.h"

/**
 * _islower - A code that check for lowercase character
 * @c: the int to be check
 * Return: Always 0.
 */

int _islower(int c)
{

	if (c >= 97 && c <= 122)
	{

	return (1);
	}
	return (0);
}
